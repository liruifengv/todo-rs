{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(Windows) Launch",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceRoot}/target/debug/rodo",
      "args": ["rm", "2"],
      "cwd": "${workspaceRoot}",
    },
    {
      "name": "(OSX) Launch",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceRoot}/target/debug/rodo",
      "args": ["rm", "2"],
      "cwd": "${workspaceRoot}",
    }
  ]
}